import React from 'react';
import './Loader.css'; // Import the CSS file for styling

const Loader = () => {
  return (
    <div className="loader-container">
      <span className="jumping-word">EVVI</span>
    </div>
  );
};

export default Loader;
